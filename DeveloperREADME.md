# 🚀 Dotty 开发环境配置指南

欢迎加入 **Dotty** 开发团队！本项目基于 **Tauri v2 + React + TypeScript** 构建。请按照以下步骤配置你的本地环境。

## 1. 基础环境安装 (Prerequisites)

请确保你的电脑安装了以下三个核心环境。

### A. Node.js & pnpm
*   **下载 Node.js:** [官网下载 LTS 版本](https://nodejs.org/) (推荐 v20 或 v22)。
*   **安装 pnpm:** 打开终端 (CMD/PowerShell)，运行：
    ```bash
    npm install -g pnpm
    ```

### B. Rust 编译环境 (最关键一步)
*   **Windows 用户:**
    1.  下载并安装 [Visual Studio C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)。安装时务必勾选 **“C++ 桌面开发”**。
    2.  下载并安装 [Rustup](https://www.rust-lang.org/tools/install)。
        *   *注意：建议配置环境变量 `RUSTUP_HOME` 和 `CARGO_HOME` 到 D 盘，否则 C 盘会爆炸。*
*   **验证:** 重启终端，输入 `cargo -v`，显示版本号即成功。

### C. 编辑器
*   推荐使用 **VS Code**。
*   必装插件：**Rust Analyzer**, **Tailwind CSS IntelliSense**, **ESLint**, **Tauri**。

---

## 2. 获取代码与安装依赖

1.  **克隆仓库:**
    ```bash
    git clone https://github.com/S7AM1NA/dotty.git
    cd dotty
    ```

2.  **安装依赖:**
    *   本项目强制使用 `pnpm`，请勿使用 npm 或 yarn。
    ```bash
    pnpm install
    ```

---

## 3. 启动项目

在终端运行以下命令：

```bash
pnpm tauri dev
```

*   **首次运行提示:** 第一次编译 Rust 后端会下载大量依赖（约 5-10 分钟），请耐心等待。
*   **成功标志:** 看到白色应用窗口弹出，且控制台无报错。

